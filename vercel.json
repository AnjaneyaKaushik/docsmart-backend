{
  "build": {
    "env": {
      "NPM_CONFIG_UPDATE_NOTIFIER": "false",
      "NPM_CONFIG_FUND": "false"
    }
  },
  "installCommand": "apt-get update && apt-get install -y --no-install-recommends build-essential libcairo2-dev libjpeg-dev libpango1.0-dev libgif-dev librsvg2-dev ghostscript python3 python3-pip && pip3 install pdf2docx && npm install",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    },
    {
      "src": "src/app/api/**/*.js",
      "use": "@vercel/node"
    }
  ]
}

